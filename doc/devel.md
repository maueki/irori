irori documentation for developer
======

install npm from apt

```
sudo apt-get install nodejs npm

sudo update-alternatives --install /usr/bin/node node /usr/bin/nodejs 10
```

install gulp and NPM modules depend on irori

```
npm install

npm install -g gulp
```

watch gulp task

```
gulp
```

